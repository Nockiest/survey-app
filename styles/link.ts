import * as stylex from "@stylexjs/stylex";

export const LINK_STYLES = stylex.create({
    base:{

        textDecoration:'none',
        color:'inherit', /* Inherit the color from the parent element */

    },

})